{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Packages",
            "type": "process",
            "command": "colcon",
            "args": ["build"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}/"
            }
        }
    ]
}